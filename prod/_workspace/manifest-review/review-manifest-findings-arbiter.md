# review: manifest-findings-arbiter

판정: PASS (만족도 94%)
검수자: make-agent-reviewer
라운드: 1

## 하드 요구사항 13/13 충족
tools 정확히 Read,Glob,Grep,Write · model opus · name==파일명 ·
입력 2경로 문자일치 · 출력 report.md 고정 + 배타성 · 최종 텍스트 3줄 형식 고정 ·
무한루프 방지 2문장 · 근거 재검증 실동작 · 심각도 재판정 · 입력 결손 시 계속 ·
headless 자족 · 수정 금지 · 최종 라운드 PASS 미전환

## 수정 지시
### 1. [필수] 커버리지 강등 규칙이 정상 케이스를 오탐 — 워크플로 상시 실패 위험
두 감사자는 발견 0건인 축을 `검사함(이상 없음)`으로 적도록 명시 지시받았는데,
그 형식에는 검사한 파일을 적는 필드가 없어 arbiter가 확인할 "흔적"이 원천적으로 부재.
현행 규칙대로면 자동 강등 → nonpass → 2라운드 소진 → 실패 종료.
team-spec L193 "PASS = 12축 전부 검사함(발견 0건도 포함)"와 실질 충돌.
→ `검사함(이상 없음)`은 원칙적으로 액면 인정.
→ 강등은 (a) 커버리지 절에 그 축 줄이 아예 없음, (b) 사유 없는 미검사,
  (c) arbiter가 Grep 스팟체크로 "이상 없음"과 명백히 배치되는 사실을 직접 확인한 경우로 한정.

### 2. 번호 없는 "매니페스트 내부 참조 일관성" 커버리지 항목 처리 미정의
매핑할 행이 없어 조용히 누락됨. 사용자 핵심 관심사(ArgoCD source.path ↔ app-node)가 여기 속함.
→ 12축 행과 별도로 한 행 추가, 담당은 manifest-static-auditor, PASS 조건에 포함.

### 3. 커버리지 절 파싱 형식이 두 감사자 간 불일치 (불릿 유무)
→ "`-` 불릿 유무·괄호 안 사유 문구 차이는 무시하고 `축 N:` 토큰과 검사함/미검사 키워드로 파싱" 추가.

### 4. `집계:` 산정 기준 미정의
→ "재판정·병합 후 채택된 발견만 센다(기각 제외, 병합은 1건). `## 요약` 건수와 반드시 일치" 추가.

## 작성자 재량 판단
- 줄번호 오차 → 문자열 실재 시 교정 채택: 타당, 유지
- 확신도 추정을 근거 부실로 기각하지 않음: 타당, 유지
- "검사함이나 흔적 없음" 강등: 의도는 타당하나 조작적 정의 불가 → 위 1번으로 재작성
- 12축 기본 배정 하드코딩: 폴백 한정이라 허용. "프롬프트가 기본 배정과 다르면 프롬프트를 따르고
  그 사실을 미해결 gap에 한 줄 남긴다" 추가 권장
